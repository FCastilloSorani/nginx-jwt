version: "3.8"

services:    
  nginx:
    build: ./nginx
    container_name: nginx
    ports:
      - "8080:80"
    restart: on-failure
    networks:
      - mi-salta-network

networks:
  mi-salta-network:
    external: true
